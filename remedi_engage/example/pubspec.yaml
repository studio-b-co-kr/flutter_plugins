name: example
description: A new Flutter application.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=2.12.0 <3.0.0'
  flutter: '>=2.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  firebase_core: ^1.0.3
  remedi_engage:
    path: ../

  remedi_base:
    git:
      url: https://github.com/studio-b-co-kr/remedi_flutter.git
      ref: achoo-release-0.2.0
      path: remedi_base/

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true
