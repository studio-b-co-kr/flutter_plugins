name: example
description: A new Flutter application.

publish_to: 'none'

version: 1.0.1+1

environment:
  sdk: ">=2.17.6 <3.0.0"
  flutter: '>=2.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  firebase_core: ^4.0.0
  remedi_engage:
    path: ../

  remedi_base: ^0.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true
