name: remedi_engage
description: Push notification, Deeplink and other tools for interaction with user.
version: 0.0.1
homepage: 'https://github.com/studio-b-co-kr/remedi_flutter_app_components'
repository: 'https://github.com/studio-b-co-kr/remedi_flutter_app_components'

environment:
  sdk: '>=2.12.0 <3.0.0'
  flutter: ">=2.0.0"

dependencies:
  flutter:
    sdk: flutter

  firebase_messaging: ^10.0.6
  flutter_local_notifications: ^8.1.1+2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter: